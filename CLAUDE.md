# 이 블로그에 대해

AIFFEL(모두의연구소)에서 AI/바이브코딩/업무자동화를 학습하며, 배운 내용과 직접 만든 프로젝트, 시행착오를 기록하는 개발일지입니다. 블로그 운영 자체가 학습 과제 중 하나입니다.

- Jekyll + GitHub Pages (remote_theme: minimal-mistakes)
- 배포 주소: https://elfinnani-byte.github.io/blog/
- 저장소 폴더(`G:\내 드라이브\blog`)를 Obsidian 볼트로 사용

## ⚠️ 최우선 절대 규칙: 참고자료를 절대 그대로 베끼지 않기

**이 규칙은 이미 최소 두 번(2편, 3편) 실제로 어겨졌다.** 강의자료 원문과 문장 순서·표현·예시 숫자까지 거의 동일한 문단을 "직접 쓴 설명"인 것처럼 작성해 발행까지 한 적이 있다. "표현을 조금 바꿨으니 괜찮겠지"는 틀린 판단이다 — 어미만 바꾸거나(-습니다 → -다) 절 순서만 뒤집거나 접속사만 바꾼 것은 베낀 것이다. 이 파일의 다른 어떤 지침보다 우선한다.

### 실제로 있었던 위반 사례 (반면교사 — 절대 반복하지 말 것)

- 신뢰도 점수 설명에서, 원문의 "안전모 탐지 0.91 / 위험구역 0.48짜리 흐릿한 박스" 예시를 문장 순서만 바꿔 그대로 씀
- IoU 계산 예시에서, 원문의 숫자(200×130, 160×112, 결과 0.53)를 그대로 가져와 "예를 들어"로만 포장함
- 원문의 인용구("여기 물체가 있을 확률", "특징 지도 위에서 잘라 쓰면 된다") 를 그대로 재사용
- 임계값 표·앵커 개수(3×3=9개) 설명에서 원문 표의 예시 상황(화재감지/자동결제)과 문장 구조를 그대로 가져옴

**숫자·예시·시나리오·인용구까지 원문과 같은데 어미나 접속사만 다른 글은, 전부 표절이지 재구성이 아니다.**

### 글을 쓸 때 무조건 지키는 절차

1. **쓰기 전에 반드시 원문을 먼저 읽는다.** `learn.modulabs.co.kr`은 로그인이 필요해 직접 fetch가 막혀 있으므로, `mcp__claude-in-chrome` 도구로 사용자의 실제 로그인된 브라우저를 통해 해당 스텝 페이지에 접속해서 읽는다("깊이 보기" 접힌 섹션도 전부 펼쳐서 확인). "이미 아는 내용이니 안 봐도 되겠지"라고 생략하지 않는다.
2. 원문을 읽은 뒤에는 **원문을 옆에 띄워놓고 문장을 고쳐 쓰지 않는다.** 원문 내용을 이해했으면 그 화면에서 벗어나, 내가 이해한 바를 내 언어로 새로 쓴다. 원문을 보면서 고쳐 쓰면 무의식중에 원문의 문장 구조·예시를 그대로 따라가게 된다.
3. **예시·숫자·등장인물·시나리오는 원문과 반드시 달라야 한다.** 공식 용어와 공식적으로 알려진 사실(예: KITTI가 차량엔 IoU 0.70, 보행자엔 0.50을 쓴다는 사실 자체)은 인용 가능하지만, 그걸 설명하는 문장·비유·구체적 예시 숫자는 매번 새로 만든다.
4. **다 쓴 뒤에는 초안과 원문을 나란히 놓고 문장·표·예시 단위로 실제 대조한다.** "느낌상 다른 것 같다"로 넘기지 않는다. 의심 가는 문장은 원문에서 실제로 검색해 확인한다.
5. 이 대조를 실제로 수행했다는 근거(어느 문단을 어떻게 비교했는지)를 사용자에게 보고한다.

의심스러우면 무조건 원문을 다시 열어 대조한다. "원래 알던 지식이라 겹칠 수밖에 없다"는 식의 판단으로 이 절차를 건너뛰지 않는다.

## 폴더 구조 (콘텐츠 워크플로우)

- `garden/` — 비공개 메모. Obsidian에서 자유롭게 위키링크로 작성. **gitignore 대상**이라 GitHub에 절대 올라가지 않고, `_config.yml`의 `exclude`에도 있어 Jekyll 빌드에도 포함되지 않음.
- `_posts/` — 실제 발행되는 글. `YYYY-MM-DD-제목.md` 형식.

## 카테고리 (3개 고정)

글은 아래 3개 카테고리 중 하나로 분류하고, 카테고리마다 글의 구조/톤을 다르게 가져간다.

| 카테고리   | 구조                       | 톤                                      |SKILL                    |
| ------ | ------------------------ | -------------------------------------- |-----------------------|
| `개념정리` | 용어 → 정의 → 예시 → 헷갈렸던 부분   | 간결, 설명체                                |                    |
| `프로젝트` | 목표 → 시도 → 막힌 점 → 해결 → 결과 | 담백한 서술체                                |                    |
| `회고`   | 상황 → 삽질 과정 → 깨달은 점       | 대화체, 담담하게  | dev-insight-post                    |

### `개념정리` 작성 시 유의사항

**해당 분야에서 실제로 쓰이는 정식 용어·개념을 기본으로 쓰고**, 그 위에 이해를 도운 예시·비유를 덧붙이는 방식으로 쓴다. 내가 이해하기 쉽게 바꿔 부른 표현만으로 글을 채우지 않는다 — 정식 용어가 빠지면 나중에 검색하거나 다른 자료와 연결할 때 쓸모가 없어진다.

- 용어 → (정식) 정의 → 비개발자도 이해할 수 있는 예시/비유 → 헷갈렸던 부분, 순서로 쓴다.
- 예시·비유는 정식 용어를 "대체"하는 게 아니라 "보충 설명"으로 붙인다. (예: "이게 바로 인덱스(index)다 — 책 맨 뒤 찾아보기처럼, 테이블 전체를 뒤지지 않고 원하는 행을 빠르게 찾게 해주는 것" — 처럼 정식 용어를 먼저 밝히고 비유로 풀어준다.)
- 참고자료 문장을 그대로 베끼지 않는 것에 대해서는 아래 "절대 규칙: 참고자료 본문을 그대로 베끼지 않기"를 반드시 함께 따른다.
- **용어는 등장 순서를 지킨다.** 뒤에서 정의할 용어를 앞 섹션에서 아무 설명 없이 미리 언급하지 않는다 (예: "분류 기술로도 부족하다"는 문장을 쓰려면, 그 전에 "분류"가 무엇인지 먼저 정의돼 있어야 한다).
- **핵심 질문이 정해져 있다면**(발표/시리즈 기획표 등) 본문 상단에 `**이 글의 핵심 질문: ~?**` 형식으로 그대로 노출한다.
- **서두는 글을 다 쓴 뒤 마지막에 다시 검토한다.** 작성 도중 섹션이 추가되거나 순서가 바뀌면 서두가 더는 본문 전체를 대표하지 못하게 되는 경우가 많다 — 발행 전 서두만 따로 다시 읽고 실제 본문 순서와 맞는지 확인한다.
- **여러 편으로 이어지는 시리즈일 때**: 1편 서두에 전체 로드맵을 인용구(`>`)로 넣고, 2편부터는 `{% post_url %}`로 1편을 링크하며 "이전 편에서 ~했다면 이번 편은 ~"으로 이어준다. 각 편 하단 "다음 편"도 `{% post_url %}`로 연결한다(파일명은 시리즈 시작 시 확정해두고 이후 바꾸지 않는다).
- **실습/직접 해본 내용이 있을 때**: 왜 그 실습을 했는지(바로 위 개념을 확인하려 했다는 것)를 한 문장으로 먼저 연결한 뒤 결과를 쓴다. 실습 섹션은 `<details markdown="1"><summary>...</summary>...</details>`로 접어 개념 설명 흐름과 분리하고, 여러 대상을 비교했다면 실제로 만든 비교 결과 파일에서 가져온 비교표를 마지막에 넣는다.
- **강의자료의 다이어그램을 그대로 쓸 때는 새로 그리지 않고 원본을 가져와도 된다**(페이지 DOM에서 SVG 소스를 그대로 추출하는 것도 가능). 이 경우 이미지 바로 아래에 `*출처: ...*` 캡션을 반드시 넣는다.

**발행 전 최종 점검**
- [ ] 기획표에 정해진 "포함할 내용" 항목이 전부 본문에 들어갔는지 하나씩 대조했는가
- [ ] 강의자료 원문과 겹치는 문장, 반대로 정식 용어를 내 맘대로 바꿔 부른 곳이 없는지 확인했는가
- [ ] 비문·어색한 전환 문장(특히 섹션 간 연결부)을 재검토했는가
- [ ] 서두가 완성된 본문의 실제 순서를 정확히 대표하는지 마지막으로 재확인했는가

## 절대 규칙: 참고자료 본문을 그대로 베끼지 않기

**강의자료·문서 등 참고자료의 문장을 그대로 옮기거나, 어미만 바꾸는 식(-습니다 → -다)으로 재구성하는 것은 절대 하지 않는다.** 카테고리를 가리지 않고 모든 글에 적용되는 절대 규칙이다.

- **정식 용어(전문 용어·개념 이름)는 그대로 쓴다.** 용어 자체를 내 맘대로 다른 말로 바꿔 부르지 않는다. (예: "값/위치"라는 용어를 "읽기/찾기"처럼 직관적으로 들리는 나만의 표현으로 바꿔서 주 용어로 쓰면 안 됨 — 그런 표현은 보충 비유로만 덧붙인다.)
- **그 용어를 설명하는 문장·비유·예시 장면은 매번 스스로 이해한 방식대로 새로 쓴다.** 참고자료가 든 예시(등장인물, 시나리오, 숫자)와 다른, 내가 만든 새로운 장면으로 같은 개념을 설명한다. (예: 참고자료가 "조립 라인 사진 + 품질팀/안전팀/설비팀" 예시를 썼다면, 나는 "매장 CCTV + 재고/보안/시설 담당자" 같은 다른 장면으로 같은 개념을 설명한다.)
- 다이어그램처럼 원본 그림 자체를 그대로 가져오는 것은 예외로 허용되지만("포스트에 그림 넣기" 참고), 이 경우도 이미지 바로 아래에 출처를 반드시 밝힌다.
- **발행 전 참고자료 원문과 초안을 나란히 놓고 겹치는 문장이 없는지 반드시 대조한다.** 이 점검을 건너뛰지 않는다.

## 절대 규칙: 새 글은 무조건 garden/ 먼저

**사용자가 어떤 표현으로 요청하든("메모로 써줘", "기록해줘", "블로그 글로 정리해줘", "삽질기 써줘", "포스트 하나 만들어줘" 등), `garden/`에 아직 없는 새로운 내용이면 무조건 `garden/`에 먼저 쓴다.** "블로그"라는 단어가 언급됐다고, 또는 글의 완성도가 높다고 곧바로 `_posts/`에 쓰면 안 된다.

`_posts/`에 직접 쓰는 것은 아래 두 조건을 **모두** 만족할 때만 예외적으로 허용된다:
1. 사용자가 "발행해줘"/"publish"처럼 **명시적인 발행 의도**를 밝혔고
2. 그 대상이 **이미 `garden/`에 존재하는 메모**를 가리킬 때

조금이라도 애매하면(발행 의도인지 신규 기록 요청인지 헷갈리면) `garden/`에 초안으로 쓰고 사용자에게 "이거 바로 발행할까요, 아니면 초안으로만 남겨둘까요?"라고 되물어본다.

## 발행 워크플로우

사용자가 "이 메모 발행해줘" 같은 요청을 하면:

1. `garden/` 안의 해당 메모를 읽는다.
2. 위 카테고리 표에 맞춰 제목/카테고리/태그와 본문 구조를 제안한다.
3. `_posts/YYYY-MM-DD-제목.md`로 파일을 작성한다 (front matter: title, categories, tags, date, description(200자 이내 요약)).
   - **이미지 1개 이상 포함** (없으면 내용에 맞는 다이어그램/일러스트를 만들어서 넣는다. "포스트에 그림 넣기" 절차 참고)
   - **강조할 부분은 bold(`**`) 또는 인용(`>`)으로 표시**해서 핵심이 눈에 띄게 한다.
4. **바로 push하지 않고, 사용자에게 내용을 보여주고 확인을 받는다.**
5. 사용자가 승인하면 그때 `git add` → `git commit` → `git push` 한다. (GitHub Pages가 push 후 자동으로 사이트에 반영)

메모를 발행 후에도 `garden/`에 남겨둘지 삭제할지는 매번 사용자에게 확인한다.

## 여러 세션 동시 작업 시 주의

이 저장소는 여러 Claude Code 세션(예: 블로그 디자인 작업 세션과 이 발행 파이프라인 세션)이 **같은 로컬 폴더·같은 `main` 브랜치를 동시에 공유**하는 경우가 있다. worktree로 분리하지 않는 한, git 커밋 시 다음을 지킨다:

- `git add -A` / `git add .` 금지 — 항상 관련 파일만 경로를 콕 집어서 스테이징한다.
- 커밋 전 `git status`로 낯선(내가 만들지 않은) 변경사항이 섞여 있는지 확인하고, 애매하면 커밋하지 말고 사용자에게 먼저 물어본다.
- push 전 `git fetch`로 다른 세션이 이미 push했는지 확인한다 (ahead/behind 상태 체크).

## 포스트에 그림 넣기

글 내용을 설명하는 간단한 다이어그램/일러스트가 필요하면:

1. `mcp__visualize__show_widget`으로 먼저 대화창에 미리보기를 그려서 사용자에게 스타일을 확인받는다 (이때는 Claude 전용 CSS 변수 사용 가능).
2. 확정되면 **같은 그림을 색상값을 하드코딩한 순수 SVG로 다시 만들어** `assets/images/파일명.svg`에 저장한다. (Claude 전용 CSS 변수는 실제 블로그 페이지에는 존재하지 않아 렌더링되지 않음)
3. 포스트 본문에는 `relative_url` 필터로 참조한다 (baseurl이 `/blog`이므로):

   ```markdown
   ![설명]({{ "/assets/images/파일명.svg" | relative_url }})
   ```

외부 서비스(GitHub user-attachments 등)에 업로드하는 방식은 쓰지 않는다 — 저장소 안에 파일로 직접 저장해서 외부 의존성을 없앤다.

이미지 정렬(가운데 정렬, 본문 너비 70%)은 `_sass/_custom-overrides.scss`의 `.page__content-inner img` 규칙으로 전역 적용되므로, 포스트에서는 그냥 일반 마크다운 이미지 문법만 쓰면 된다 (별도 스타일 태그 불필요).

## 정보 박스(callout) 넣기

팁·주의·참고 등을 강조하고 싶으면 Minimal Mistakes 테마에 내장된 `.notice--*` 클래스를 쓴다 (별도 CSS 불필요).

여러 문단(제목+본문)이 들어갈 때:

```markdown
<div class="notice--info" markdown="1">
**ⓘ 제목**

본문 내용. **bold** 같은 마크다운도 여기 그대로 쓸 수 있음.
</div>
```

한 문단짜리 짧은 박스일 때:

```markdown
**ⓘ 제목** — 한 줄 내용.
{: .notice--info}
```

`markdown="1"`을 빠뜨리면 안의 마크다운 문법이 처리되지 않으니 항상 포함한다. 색상은 `.notice`(회색) / `.notice--primary`(강조색) / `.notice--info`(파랑) / `.notice--warning`(노랑) / `.notice--success`(초록) / `.notice--danger`(빨강) 중 내용 성격에 맞게 고른다.
